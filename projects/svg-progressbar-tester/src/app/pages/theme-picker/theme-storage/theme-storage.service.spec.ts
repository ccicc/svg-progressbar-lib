import { ThemeStorageService } from './theme-storage.service';

describe('ThemeStorage', () => {
  it('should create an instance', () => {
    expect(new ThemeStorageService()).toBeTruthy();
  });
});
